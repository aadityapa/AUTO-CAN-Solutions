/* ============================================================
   DEPRECATED — the /v2 concept page has been merged into the
   main site (see src/components/XpSections.jsx, used by Home).
   This file is no longer routed or bundled. Safe to delete.
   ============================================================ */
export default function Experience() {
  return null
}
